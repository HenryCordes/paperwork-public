# Deployment

Read this for deploy/runtime/Heroku questions.

- Deploys to Heroku (see `Procfile`).
- Validate that architecture choices hold true in Heroku's production
  environment (e.g. ephemeral filesystem, dyno restarts, Redis/Mongo add-ons).
